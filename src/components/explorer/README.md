# Explorer components

Reserved for the list/detail file-explorer surface that complements the galaxy
view (table mode, column view, drag & drop targets, context menus).

Intentionally empty for now — the first iteration ships the galaxy view only.
Components added here should stay presentational and read from the same stores
as `components/galaxy`, so both views can render the same directory at once.
